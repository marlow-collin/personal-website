import { adminApi } from "/x/admin/shared/api.js";
const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));const nav=$("[data-section-nav]");if(nav)nav.addEventListener("change",()=>location.href=nav.value);

const metrics=$("[data-metrics]"),cats=$("[data-categories]"),meta=$("[data-meta]");
async function load(){const d=await adminApi("/conversation/status"),s=d.summary;metrics.innerHTML=`<div class="metric"><span>Total</span><strong>${s.total}</strong></div><div class="metric"><span>Active</span><strong>${s.active}</strong></div><div class="metric"><span>Archived</span><strong>${s.archived}</strong></div><div class="metric"><span>3+ only</span><strong>${s.groupOnly}</strong></div>`;const im=Object.fromEntries(s.byIntensity.map(x=>[x.intensity,x.count]));cats.innerHTML=s.byCategory.map(c=>`<article class="category-card"><h3>${esc(c.category)}</h3><div class="mini-metrics"><div class="mini-metric"><span>Active</span><strong>${c.count}</strong></div></div></article>`).join("");meta.innerHTML=`<div class="section-heading"><h2>Active pool metadata</h2></div><p class="muted-line">Intensity: ${s.byIntensity.map(x=>`${esc(x.intensity)} ${x.count}`).join(" · ")}</p><p class="muted-line">Topics: ${s.byTopic.length} represented · Contexts: ${s.byContext.map(x=>`${esc(x.context)} ${x.count}`).join(" · ")||"universal only"}</p>`}
$("[data-refresh]").addEventListener("click",()=>load());await load();
